# Snow flakes プロジェクト — Claude 永続ルール

## 自律実行ルール

以下の操作はユーザー確認なしで自律実行する。

- 読み取り・状態確認
- バックアップ作成
- 既存 migration 機構による DB migration
- テスト実行
- dry-run
- ログ確認
- 安全な修正（既存コードへの最小変更）

「実行してよいですか」「次へ進めますか」「バックアップしますか」などの途中確認はしない。

安全な選択肢が明確なら自分で判断して進める。

作業完了後に一度だけ結果を報告する。途中経過報告は不要。

---

## JARVIS 開発の担当・二重チェック運用

JARVIS 本体の通常開発・修正は **Claude を主担当**とする。
ChatGPT 側での本体コード直接変更は、Claude が利用できない緊急時・出先対応などに限定する。

Claude は JARVIS 作業を始める前に必ず以下を行う。

1. `jarvis/CLAUDE_AUDIT_2026-08-18.md` が存在する場合は最初に読む。
2. `jarvis/JARVIS_CHANGELOG.md` を読み、直近変更と未確認項目を把握する。
3. ChatGPT・手動・他エージェントによる直近変更がある場合、**その変更を信用して上書きで進めず、現行コード・読み込み順・イベント競合・実際にロードされるファイルまで監査する**。
4. 修正前に要求と現状実装の差分を洗い出す。
5. 修正後は関連機能をまとめて回帰確認し、今回触っていない隣接機能も壊れていないか確認する。
6. JARVIS のコード変更は、大小を問わず `jarvis/JARVIS_CHANGELOG.md` に必ず追記する。変更ファイル、目的、確認結果、未確認事項、commit SHA（commit 後）を残す。

**「画面上では直ったように見える」だけで完了扱いにしない。**
DOM を後から書き換える複数スクリプト、script injection、MutationObserver、イベントリスナー競合、古い互換コードが残っていないかまで確認する。

特に Business ダッシュボードは、同じナビや UI を複数ファイルから変更しない。責務を整理し、可能なら一か所に集約してから修正する。

---

## 停止してユーザー確認が必要な場合（以下のみ）

1. branch / HEAD が指定と異なる
2. 想定外の追跡ファイル変更がある
3. データ削除・破損リスクがある
4. 外部への公開・送信・課金が発生する
5. secret / token / 認証情報をユーザー自身が入力する必要がある
6. 指示が矛盾して安全に判断できない

---

## git ルール（全プロジェクト共通）

- `git add` / `commit` / `push` / `main へのマージ` はユーザー承認後のみ実行する
- 対象範囲: jarvis・snowflakes サイト・taskpwa すべて同じ承認制
- `git add .` は禁止。追加ファイルは個別指定する
- 未追跡の音声・画像・CSV・DB-WAL/SHM・backups には触れない

作業（migration・修正・テスト等）は承認なしで自律実行する。
Git へ確定する操作（add / commit / push）のみ承認制。

## push 承認後のバージョン更新（taskpwa）

taskpwa への push 承認後は `app.js` バージョンと `sw.js` キャッシュを必ず更新する。
バージョン形式: `1.01` 形式（例: 1.02、1.03）。
